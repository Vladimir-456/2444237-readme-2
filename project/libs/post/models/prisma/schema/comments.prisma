datasource db_comment {
    provider = "postgresql"
}

model Comment {
    id     String @id @default(uuid())
    text   String
    author String
    postId String @map("post_id")
    post   Post   @relation(fields: [postId], references: [id], onDelete: Cascade, onUpdate: Cascade)

    @@map("comments")
}
